import { Clock3, LogOut, ShieldCheck } from 'lucide-react';
import { useAuth } from '../context/AuthContext';

export default function SessionTimeoutModal() { const { sessionWarning, staySignedIn, logout } = useAuth(); if (!sessionWarning) return null; return <div className="fixed inset-0 z-[60] grid place-items-center bg-slate-950/45 p-4"><div className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-start gap-3"><span className="grid h-11 w-11 place-items-center rounded-xl bg-amber-50 text-amber-700"><Clock3 size={21} /></span><div><p className="text-xs font-bold uppercase tracking-widest text-amber-700">Session security</p><h2 className="mt-1 text-xl font-bold text-slate-900">Are you still working?</h2></div></div><p className="mt-5 text-sm leading-6 text-slate-600">For the safety of sensitive case information, you will be signed out soon because there has been no activity.</p><div className="mt-5 flex items-center gap-2 rounded-xl bg-teal-50 p-3 text-xs font-semibold text-teal-800"><ShieldCheck size={16} />Your session will remain private.</div><div className="mt-6 flex justify-end gap-2"><button onClick={logout} className="inline-flex items-center gap-2 rounded-xl px-4 py-2.5 text-sm font-bold text-slate-500 hover:bg-slate-50"><LogOut size={16} />Sign out</button><button onClick={staySignedIn} className="rounded-xl bg-[#0b4f4b] px-4 py-2.5 text-sm font-bold text-white">Stay signed in</button></div></div></div>; }