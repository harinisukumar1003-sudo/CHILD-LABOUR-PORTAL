import { ClipboardList, Filter, Search, ShieldCheck } from 'lucide-react';
import { useEffect, useState } from 'react';
import api from '../services/api';

const sample = [{ id: 1, adminUserId: 3, action: 'APPROVE_USER', targetEntity: 'User', targetId: 23, timestamp: '2026-08-24T09:32:00Z', ipAddress: '192.168.1.18' }, { id: 2, adminUserId: 3, action: 'EXPORT_REPORTS_PDF', targetEntity: 'CaseReport', targetId: null, timestamp: '2026-08-23T16:18:00Z', ipAddress: '192.168.1.18' }, { id: 3, adminUserId: 3, action: 'SUSPEND_USER', targetEntity: 'User', targetId: 24, timestamp: '2026-08-22T11:05:00Z', ipAddress: '192.168.1.18' }, { id: 4, adminUserId: 3, action: 'CHANGE_USER_ROLE', targetEntity: 'User', targetId: 22, timestamp: '2026-08-21T14:42:00Z', ipAddress: '192.168.1.18' }];
const actionLabel = value => value.replaceAll('_', ' ').toLowerCase().replace(/\b\w/g, char => char.toUpperCase());
export default function AdminAuditLog() { const [items, setItems] = useState(sample); const [query, setQuery] = useState(''); const [action, setAction] = useState(''); useEffect(() => { api.get('/admin/audit-logs', { params: { size: 100 } }).then(response => setItems(response.data.data?.content || sample)).catch(() => {}); }, []); const visible = items.filter(item => `${item.action} ${item.targetEntity} ${item.targetId || ''} ${item.ipAddress || ''}`.toLowerCase().includes(query.toLowerCase()) && (!action || item.action === action)); return <div className="space-y-7 pb-10"><header><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.18em] text-teal-700"><ShieldCheck size={15} />Compliance centre</div><h1 className="mt-2 text-3xl font-bold tracking-tight text-slate-900">Audit log</h1><p className="mt-2 text-sm text-slate-500">A durable record of administrative decisions and official exports.</p></header><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-slate-100 p-4 sm:flex-row"><div className="relative flex-1"><Search size={17} className="absolute left-3 top-2.5 text-slate-400" /><input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search actions, entities, IP addresses..." className="w-full rounded-xl bg-slate-50 py-2.5 pl-9 text-sm outline-none focus:ring-2 focus:ring-teal-100" /></div><div className="relative"><Filter size={15} className="absolute left-3 top-3 text-slate-400" /><select value={action} onChange={event => setAction(event.target.value)} className="rounded-xl border border-slate-200 bg-white py-2.5 pl-9 pr-8 text-xs font-bold text-slate-600"><option value="">All actions</option>{[...new Set(items.map(item => item.action))].map(item => <option key={item}>{item}</option>)}</select></div></div><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-slate-50 text-[10px] uppercase tracking-wider text-slate-400"><tr><th className="px-5 py-3 font-bold">Action</th><th className="px-5 py-3 font-bold">Target</th><th className="px-5 py-3 font-bold">Administrator</th><th className="px-5 py-3 font-bold">Time</th><th className="px-5 py-3 font-bold">IP address</th></tr></thead><tbody>{visible.map(item => <tr key={item.id} className="border-t border-slate-100"><td className="px-5 py-4"><span className="inline-flex items-center gap-2 font-bold text-slate-700"><span className="grid h-7 w-7 place-items-center rounded-lg bg-teal-50 text-teal-700"><ClipboardList size={14} /></span>{actionLabel(item.action)}</span></td><td className="px-5 py-4 text-xs font-semibold text-slate-500">{item.targetEntity}{item.targetId ? ` #${item.targetId}` : ''}</td><td className="px-5 py-4 text-xs font-semibold text-slate-600">Admin #{item.adminUserId}</td><td className="px-5 py-4 text-xs text-slate-500">{new Date(item.timestamp).toLocaleString('en-IN')}</td><td className="px-5 py-4 font-mono text-xs text-slate-400">{item.ipAddress || 'Not recorded'}</td></tr>)}</tbody></table></div>{!visible.length && <p className="p-12 text-center text-sm text-slate-400">No audit entries match your search.</p>}</section></div>; }