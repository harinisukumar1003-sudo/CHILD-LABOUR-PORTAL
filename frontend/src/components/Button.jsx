export default function Button({ children, variant = 'primary', ...props }) { const styles = { primary: 'bg-primary text-white hover:bg-teal-800', ghost: 'text-neutral-700 hover:bg-neutral-100', amber: 'bg-amber-50 text-amber-700 hover:bg-amber-100' }; return <button className={`rounded-xl px-4 py-2.5 text-sm font-semibold transition ${styles[variant]}`} {...props}>{children}</button>; }
