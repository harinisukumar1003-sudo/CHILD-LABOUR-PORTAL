export const notificationIcons = { NEW_REPORT: 'NEW_REPORT', CASE_ASSIGNED: 'CASE_ASSIGNED', STATUS_CHANGED: 'STATUS_CHANGED', FOLLOW_UP_DUE: 'FOLLOW_UP_DUE', ACCOUNT_APPROVED: 'ACCOUNT_APPROVED' };
export const notificationLabels = { NEW_REPORT: 'New report', CASE_ASSIGNED: 'Case assigned', STATUS_CHANGED: 'Status changed', FOLLOW_UP_DUE: 'Follow-up due', ACCOUNT_APPROVED: 'Account approved' };
export const notificationColors = { NEW_REPORT: 'bg-sky-50 text-sky-700', CASE_ASSIGNED: 'bg-violet-50 text-violet-700', STATUS_CHANGED: 'bg-teal-50 text-teal-700', FOLLOW_UP_DUE: 'bg-amber-50 text-amber-700', ACCOUNT_APPROVED: 'bg-emerald-50 text-emerald-700' };
export const relativeTime = value => { const seconds = Math.max(1, Math.floor((Date.now() - new Date(value).getTime()) / 1000)); if (seconds < 60) return `${seconds}m ago`; const minutes = Math.floor(seconds / 60); if (minutes < 60) return `${minutes}h ago`; const hours = Math.floor(minutes / 60); if (hours < 24) return `${hours}h ago`; return `${Math.floor(hours / 24)}d ago`; };