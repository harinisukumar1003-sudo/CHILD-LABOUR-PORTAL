import { Building2, Edit3, MapPin, Phone, Plus, Search, Trash2, Users, X } from 'lucide-react';
import { useEffect, useState } from 'react';
import api from '../services/api';

const sampleCenters = [{ id: 1, name: 'Udaan Child Support Centre', address: 'Kothrud, Pune', contactPerson: 'Nisha Kulkarni', contactPhone: '+91 20 4123 7744', capacity: 40, servicesOffered: 'Education, Counselling, Family Support' }, { id: 2, name: 'Sahyadri Care Home', address: 'Pimpri, Pune', contactPerson: 'Vikram Deshmukh', contactPhone: '+91 20 2890 1188', capacity: 28, servicesOffered: 'Healthcare, Counselling, Legal Aid' }, { id: 3, name: 'Nayi Disha Learning Hub', address: 'Hadapsar, Pune', contactPerson: 'Asha Patil', contactPhone: '+91 20 2673 4012', capacity: 24, servicesOffered: 'Education, Vocational Training' }];
function CenterModal({ center, onClose, onSave }) { const [form, setForm] = useState(center || { name: '', address: '', contactPerson: '', contactPhone: '', capacity: 20, servicesOffered: '' }); const update = key => event => setForm({ ...form, [key]: event.target.value }); const submit = event => { event.preventDefault(); onSave(form); }; return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/45 p-4"><div className="max-h-[90vh] w-full max-w-lg overflow-y-auto rounded-2xl bg-white p-6 shadow-2xl"><div className="flex justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-teal-700">Directory management</p><h2 className="mt-1 text-xl font-bold">{center ? 'Edit centre' : 'Add centre'}</h2></div><button onClick={onClose} aria-label="Close centre form"><X size={20} className="text-slate-400" /></button></div><form onSubmit={submit} className="mt-6 grid gap-4"><Input label="Centre name" value={form.name} onChange={update('name')} required /><Input label="Address" value={form.address} onChange={update('address')} required /><div className="grid gap-4 sm:grid-cols-2"><Input label="Contact person" value={form.contactPerson} onChange={update('contactPerson')} required /><Input label="Contact phone" value={form.contactPhone} onChange={update('contactPhone')} required /></div><Input label="Capacity" type="number" value={form.capacity} onChange={update('capacity')} required /><Input label="Services offered" value={form.servicesOffered} onChange={update('servicesOffered')} placeholder="Education, Counselling, Healthcare" required /><div className="flex justify-end gap-2 pt-2"><button type="button" onClick={onClose} className="rounded-xl px-4 py-2.5 text-sm font-bold text-slate-500">Cancel</button><button className="rounded-xl bg-[#0b4f4b] px-4 py-2.5 text-sm font-bold text-white">Save centre</button></div></form></div></div>; }
function Input({ label, type = 'text', ...props }) { return <label className="block text-sm font-bold text-slate-700">{label}<input type={type} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm font-normal outline-none focus:border-teal-500 focus:ring-4 focus:ring-teal-50" {...props} /></label>; }
export default function RehabilitationCenters() { const [centers, setCenters] = useState(sampleCenters); const [query, setQuery] = useState(''); const [modal, setModal] = useState(null); useEffect(() => { api.get('/rehabilitation/centers').then(response => { if (Array.isArray(response.data.data) && response.data.data.length) setCenters(response.data.data); }).catch(() => {}); }, []); const visible = centers.filter(center => `${center.name} ${center.address} ${center.servicesOffered}`.toLowerCase().includes(query.toLowerCase())); const save = async center => { const editing = Boolean(center.id); await api({ method: editing ? 'put' : 'post', url: editing ? `/rehabilitation/centers/${center.id}` : '/rehabilitation/centers', data: { ...center, capacity: Number(center.capacity) } }).catch(() => null); setCenters(current => editing ? current.map(item => item.id === center.id ? { ...center, capacity: Number(center.capacity) } : item) : [...current, { ...center, id: Date.now(), capacity: Number(center.capacity) }]); setModal(null); }; const remove = async id => { await api.delete(`/rehabilitation/centers/${id}`).catch(() => null); setCenters(current => current.filter(center => center.id !== id)); }; return <div className="space-y-7 pb-8"><header className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.18em] text-teal-700"><Building2 size={15} />Care network</div><h1 className="mt-2 text-3xl font-bold tracking-tight text-slate-900">Rehabilitation centres</h1><p className="mt-2 text-sm text-slate-500">Trusted places for care, learning, recovery, and a next chapter.</p></div><button onClick={() => setModal({})} className="inline-flex items-center gap-2 rounded-xl bg-[#0b4f4b] px-4 py-2.5 text-sm font-bold text-white"><Plus size={17} />Add centre</button></header><div className="relative rounded-2xl border border-slate-200 bg-white p-3 shadow-sm"><Search size={18} className="absolute left-6 top-5 text-slate-400" /><input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search centres, services, or area..." className="w-full rounded-xl bg-slate-50 py-2.5 pl-10 pr-3 text-sm outline-none focus:ring-2 focus:ring-teal-100" /></div><div className="grid gap-5 lg:grid-cols-2 xl:grid-cols-3">{visible.map(center => <CenterCard key={center.id} center={center} onEdit={() => setModal(center)} onDelete={() => remove(center.id)} />)}</div>{!visible.length && <p className="rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center text-sm text-slate-500">No centres match your search.</p>}{modal && <CenterModal center={modal.id ? modal : null} onClose={() => setModal(null)} onSave={save} />}</div>; }
function CenterCard({ center, onEdit, onDelete }) { const services = center.servicesOffered.split(',').map(item => item.trim()).filter(Boolean); const used = Math.round(center.capacity * .65); return <article className="group rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition hover:border-teal-300 hover:shadow-md"><div className="flex items-start justify-between gap-3"><span className="grid h-11 w-11 place-items-center rounded-xl bg-teal-50 text-teal-700"><Building2 size={21} /></span><div className="flex gap-1 opacity-0 transition group-hover:opacity-100"><button onClick={onEdit} aria-label={`Edit ${center.name}`} className="rounded-lg p-2 text-slate-400 hover:bg-slate-100 hover:text-teal-700"><Edit3 size={15} /></button><button onClick={onDelete} aria-label={`Delete ${center.name}`} className="rounded-lg p-2 text-slate-400 hover:bg-red-50 hover:text-red-600"><Trash2 size={15} /></button></div></div><h2 className="mt-5 text-base font-bold text-slate-800">{center.name}</h2><p className="mt-2 flex items-start gap-1.5 text-xs leading-5 text-slate-500"><MapPin size={14} className="mt-0.5 shrink-0" />{center.address}</p><div className="mt-5"><div className="flex items-center justify-between text-xs"><span className="flex items-center gap-1.5 font-bold text-slate-600"><Users size={14} />Capacity</span><span className="font-bold text-teal-700">{used} / {center.capacity} places</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div className={`h-full rounded-full ${used / center.capacity > .8 ? 'bg-amber-500' : 'bg-gradient-to-r from-emerald-400 to-teal-600'}`} style={{ width: `${Math.min(100, used / center.capacity * 100)}%` }} /></div></div><div className="mt-5 flex flex-wrap gap-1.5">{services.map(service => <span key={service} className="rounded-full bg-emerald-50 px-2.5 py-1 text-[10px] font-bold text-emerald-800">{service}</span>)}</div><div className="mt-5 border-t border-slate-100 pt-4 text-xs text-slate-500"><p className="font-bold text-slate-700">{center.contactPerson}</p><p className="mt-1 flex items-center gap-1.5"><Phone size={13} />{center.contactPhone}</p></div></article>; }