export default function Table({ columns = [], rows = [] }) { return <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr>{columns.map((column) => <th className="border-b px-4 py-3 text-xs uppercase text-slate-400" key={column.key}>{column.label}</th>)}</tr></thead><tbody>{rows.map((row, index) => <tr className="border-b" key={row.id || index}>{columns.map((column) => <td className="px-4 py-3" key={column.key}>{row[column.key]}</td>)}</tr>)}</tbody></table></div>; }
